[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["dotnet publish src/Andy.Auth.Server/Andy.Auth.Server.csproj -c Release -o out --self-contained true -r linux-x64"]

[start]
cmd = "./out/Andy.Auth.Server"
